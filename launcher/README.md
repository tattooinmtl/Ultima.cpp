# Ultima launcher (stub)

Go / Wails GUI for settings, model management, and runtime supervision. See Decision 03 in `../roadmap01.md`.

Not yet initialized. When ready:

```powershell
cd launcher
wails init -n ultima-launcher -t svelte-ts
```

This will create the Wails project scaffold. From there we wire up the settings tabs described in Decision 03 §3.5.
