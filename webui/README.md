# Ultima webui (stub)

Static assets for the browser-served chat UI. Served by `ultima serve` at `http://localhost:7777`. See Decision 03b in `../roadmap01.md`.

Contents (added at Decision 15 / M8):

- `index.html`   entry
- `assets/`      compiled JS/CSS bundles
- `favicon.ico`

Frontend build system TBD (Vite + Svelte or Vue or vanilla; deferred).
