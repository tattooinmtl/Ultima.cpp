# Placeholder. All third-party fetching happens in cmake/UltimaDeps.cmake so
# each module can declare its deps closer to its own build definition.
#
# This directory holds:
#   third_party/bin/<platform>/   bundled binaries (ripgrep, fd) — added post-scaffold
#   third_party/webui/            static assets (added Decision 15)
